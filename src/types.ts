export interface PackagesType {
    id: string;
    title: string;
    name?: string;
    author?: string;
    description?: string;
    url?: string;
}

export interface DomainsType {
    id: string;
    available?: string;
}
